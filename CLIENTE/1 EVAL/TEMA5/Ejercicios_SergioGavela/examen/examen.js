const array1 = [1, 3, 2];
// Ejemplo de uso del sort
array1.sort(function(a, b){return a - b});

function compare(a, b) {
    if (a es menor que b según criterio de ordenamiento) {
      return -1;
    }
    if (a es mayor que b según criterio de ordenamiento) {
      return 1;
    }
    // a debe ser igual b
    return 0;
  }


array1.reverse()
  